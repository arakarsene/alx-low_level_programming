#include "main.h"

/**
 * char _strcat a functions that concatenates two strings 
 * @desc: copy to 
 * @src: copy from
 * Return: two concat strings
 */
char *_strcat(char *dest, char *src)
{
	int i,m;
	for(i = 0; dest[i] != '\0'; i++)
	{
	}
	for(m = 0; (dest[i+m] + *src++) != '\0'; m++)
	{
	}

	return(dest);
}
