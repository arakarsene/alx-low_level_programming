#include <stdio.h>

/**
 * main - assigns a random number to int n everytime
 * it executes, and prints it
 * Return: Always 0 (Success)
 */
int main(void)
{
	char lt;

	for (lt = 'a' ;lt <= 'z' ; lt++)
		putchar(lt);
	
	putchar('\n');

	return (0);
}
